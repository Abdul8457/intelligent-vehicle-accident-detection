# Arduino Firmware

This directory contains the embedded-side reference implementation.

The first portfolio version keeps the core detection logic in Python so it can be tested reproducibly on a PC. Arduino integration can later be connected to real sensor interfaces.

Do not connect this prototype to real emergency services without appropriate hardware validation and safety review.
